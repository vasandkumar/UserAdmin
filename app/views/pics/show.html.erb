<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="#">
         <a class="navbar-brand" href="#"><%= @pic.imageuri %></a>
      </a>
    </div>
  </div>
</nav>
<div class="container">
	<p id="notice"><%= notice %></p>

<p>
  <strong>Imageuri:</strong>
  <%= @pic.imageuri %>
</p>

<p>
  <strong>User:</strong>
  <%= @pic.user_id %>
</p>

<p>
  <b>Picture:</b>
  <%= image_tag @pic.picture.url %>
</p>

<%= link_to 'Edit', edit_user_pic_path(id: @pic.id), style: 'margin-left: 45%', class: 'btn btn-default' %> |
<%= link_to 'Back', user_pics_path, class: 'btn btn-default' %>
